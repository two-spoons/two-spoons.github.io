# Local Development Settings
DEBUG=True
SECRET_KEY=your-secret-key-here

# Database Settings (for local development, SQLite is used by default)
# Set USE_POSTGRES=True to use PostgreSQL with Docker
USE_POSTGRES=False

# PostgreSQL Settings (only used when USE_POSTGRES=True)
DB_NAME=blog_db
DB_USER=blog_user
DB_PASSWORD=blog_password
DB_HOST=db
DB_PORT=5432